<h1>Edit First time</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.first_time_path(@conn, :update, @first_time)) %>

<span><%= link "Back", to: Routes.first_time_path(@conn, :index) %></span>
